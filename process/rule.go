package process

import (
	"context"
	"time"

	c "github.com/jerome0000/draw/conf"
)

// RuleHandler rule handler
func RuleHandler(ctx context.Context, reqTime time.Time, info *c.Info, conf *c.Conf, params map[string]interface{}) error {
	return nil
}
