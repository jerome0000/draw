package process

import (
	"context"
	"time"

	c "github.com/jerome0000/draw/conf"
)

// StockHandler stock handler
func StockHandler(ctx context.Context, reqTime time.Time, info *c.Info, conf *c.Conf, params map[string]interface{}) error {
	return nil
}
